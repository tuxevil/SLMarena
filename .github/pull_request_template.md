## What does this change?

Summarize the change and the problem it solves.

## Related issue

Fixes #<issue-number> (or "No related issue").

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] `npm test` passes
- [ ] `npm run build` passes
- [ ] E2E tests updated or added when user-facing behavior changed
- [ ] README/CHANGELOG updated when user-facing behavior changed

## Screenshots (optional)

If the change affects the UI, add before/after screenshots.
